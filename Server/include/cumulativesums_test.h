#ifndef CUMULATIVESUMS_TEST_H
#define CUMULATIVESUMS_TEST_H

#include <string>
#include <vector>

// Driver function to process the Cumulative Sums Test for multiple segments
std::string CumulativeSumsTest(const std::string &binaryFilePath, int segmentSize, int numberOfSegments);

#endif // CUMULATIVESUMS_TEST_H
