#ifndef BLOCK_FREQUENCY_TEST_H
#define BLOCK_FREQUENCY_TEST_H

#include <string>

// BlockFrequencyTest function that accepts the file path, segment size, and number of segments
std::string BlockFrequencyTest(const std::string &binaryFilePath, int segmentSize, int numberOfSegments);

#endif
