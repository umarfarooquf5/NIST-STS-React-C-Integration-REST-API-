#ifndef APPROXIMATEENTROPY_TEST_H
#define APPROXIMATEENTROPY_TEST_H

#include <string>
#include <vector>

// Driver function to process the Approximate Entropy test
std::string ApproximateEntropyTest(const std::string &binaryFilePath, int segmentSize, int numberOfSegments);

#endif // APPROXIMATEENTROPY_TEST_H
